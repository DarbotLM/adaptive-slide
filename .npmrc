; React is tracked on the 19.3 canary line for the documentation site.
; Docusaurus and @mdx-js/react declare stable-only React peer ranges, so peer
; resolution must be relaxed for both `npm install` and `npm ci`.
legacy-peer-deps=true
